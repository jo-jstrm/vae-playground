# Short config for smoke test
model:
  img_size: 32
  input_dim: 3
  hidden_dims: [16, 32, 64]
  latent_dim: 64

train:
  num_epochs: 1
  batch_size: 8
  num_workers: 0
  learn_rate: 1e-5
  val_freq: 1
  checkpoint_freq: 1

data:
  train_split: 0.8

test:
  batch_size: 8
  num_workers: 0
